//Exercsise 1-6. Verify that the expression getchar() != EOF is 0 or 1.

/* If getchar() returns a valid character (i.e., a positive integer),
 * getchar() != EOF will evaluate to 1 because the character is not equal to EOF.
 * If getchar() returns EOF (which indicates the end of input or an error),
 * the expression getchar() != EOF will evaluate to 0 because EOF != EOF is false.
*/
